# Don't put anything else in this top-level file, as otherwise it may become
# impossible for setup.py to import the version number during build.
from example_service.version import __version__, __version_info__  # noqa
